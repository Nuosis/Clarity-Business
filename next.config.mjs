/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    
    // Enable For Static Build Export
    // images: {
    //     unoptimized: true,
    // },
    // trailingSlash: true,
    // output: 'export',
    // basePath: "/templates/swift/next-tailwind",
};

export default nextConfig;
